# Mockups (approved designs)

Open each file in a browser or read the markup. Ignore the design-tool wrappers
(`<script src="./support.js">`, `<x-dc>`, `<helmet>`, `<script type="text/x-dc">`);
everything else is plain HTML with inline styles.

| file | screen | size |
|---|---|---|
| Main.dc.html | Coach · plan builder (desktop) | 1440 × 1500 |
| PdfPage1.dc.html | Client PDF · page 1 | A4, 794 × 1123 px |
| PdfPage2.dc.html | Client PDF · page 2 | A4, 794 × 1123 px |
| FuelToday.dc.html | Client · Fuel today | 390 wide phone |
| FuelMeal.dc.html | Client · tick foods + swap sheet | 390 × 844 |
| FuelExtra.dc.html | Client · type an extra | 390 × 844 |
